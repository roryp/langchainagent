# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: langchain4j-getting-started
metadata:
  template: langchain4j-getting-started@0.0.1

services:
  app:
    project: .
    language: java
    host: containerapp
    docker:
      path: ./Dockerfile
      context: ..
